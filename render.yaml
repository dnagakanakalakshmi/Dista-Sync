services:
  # Backend API Service
  - type: web
    name: dista-sync-api
    env: node
    buildCommand: cd server && npm install
    startCommand: cd server && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
      - key: MONGO_URI
        sync: false
    healthCheckPath: /api/health

  # Frontend Static Site
  - type: web
    name: dista-sync-client
    env: static
    buildCommand: cd client && npm install && npm run build
    staticPublishPath: client/dist
    envVars:
      - key: VITE_API_URL
        sync: false
    routes:
      - type: rewrite
        source: /api/*
        destination: https://dista-sync-api.onrender.com/api/*
      - type: rewrite
        source: /*
        destination: /index.html
